import LayoutContainerComponent from './index.vue'

const LayoutContainerTip = {
    install: function(Vue) {
        Vue.component('LayoutContainerTip', LayoutContainerComponent)
    }
}

export default LayoutContainerTip